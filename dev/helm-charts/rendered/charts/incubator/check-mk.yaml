---
# Source: check-mk/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: RELEASE-NAME-check-mk
  labels:
    app: check-mk
    chart: check-mk-0.2.1
    release: RELEASE-NAME
    heritage: Tiller
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      name: check-mk
  selector:
    app: check-mk
    release: RELEASE-NAME

---
# Source: check-mk/templates/deployment.yaml
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: RELEASE-NAME-check-mk
  labels:
    app: check-mk
    chart: check-mk-0.2.1
    release: RELEASE-NAME
    heritage: Tiller
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: check-mk
        release: RELEASE-NAME
    spec:
      containers:
        - name: check-mk
          image: "nlmacamp/check_mk:latest"
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
          livenessProbe:
            httpGet:
              path: /
              port: 5000
          readinessProbe:
            httpGet:
              path: /
              port: 5000
          resources:
            {}
            

---
# Source: check-mk/templates/ingress.yaml

