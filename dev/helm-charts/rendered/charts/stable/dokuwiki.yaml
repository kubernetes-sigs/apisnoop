---
# Source: dokuwiki/templates/secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: RELEASE-NAME-dokuwiki
  labels:
    app: dokuwiki
    chart: dokuwiki-4.0.0
    release: "RELEASE-NAME"
    heritage: "Tiller"
type: Opaque
data:
  
  dokuwiki-password: "VWRSVzNQYmdRbQ=="
  

---
# Source: dokuwiki/templates/apache-pvc.yaml
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: RELEASE-NAME-dokuwiki-apache
  labels:
    app: dokuwiki
    chart: dokuwiki-4.0.0
    release: "RELEASE-NAME"
    heritage: "Tiller"
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"
---
# Source: dokuwiki/templates/dokuwiki-pvc.yaml
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: RELEASE-NAME-dokuwiki-dokuwiki
  labels:
    app: dokuwiki
    chart: dokuwiki-4.0.0
    release: "RELEASE-NAME"
    heritage: "Tiller"
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "8Gi"
---
# Source: dokuwiki/templates/svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: RELEASE-NAME-dokuwiki
  labels:
    app: dokuwiki
    chart: dokuwiki-4.0.0
    release: "RELEASE-NAME"
    heritage: "Tiller"
spec:
  type: LoadBalancer
  externalTrafficPolicy: "Cluster"
  ports:
  - name: http
    port: 80
    targetPort: http
  - name: https
    port: 443
    targetPort: https
  selector:
    app: dokuwiki

---
# Source: dokuwiki/templates/deployment.yaml
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: RELEASE-NAME-dokuwiki
  labels:
    app: dokuwiki
    chart: dokuwiki-4.0.0
    release: "RELEASE-NAME"
    heritage: "Tiller"
spec:
  selector:
    matchLabels:
      app: dokuwiki
      release: "RELEASE-NAME"
  template:
    metadata:
      labels:
        app: dokuwiki
        chart: dokuwiki-4.0.0
        release: "RELEASE-NAME"
    spec:
      hostAliases:
      - ip: "127.0.0.1"
        hostnames:
        - "status.localhost"
      containers:
      - name: RELEASE-NAME-dokuwiki
        image: docker.io/bitnami/dokuwiki:0.20180422.201805030840
        imagePullPolicy: "IfNotPresent"
        env:
        - name: DOKUWIKI_USERNAME
          value: "user"
        - name: DOKUWIKI_FULL_NAME
          value: "User Name"
        - name: DOKUWIKI_PASSWORD
          valueFrom:
            secretKeyRef:
              name: RELEASE-NAME-dokuwiki
              key: dokuwiki-password
        - name: DOKUWIKI_EMAIL
          value: "user@example.com"
        - name: DOKUWIKI_WIKI_NAME
          value: "My Wiki"
        ports:
        - name: http
          containerPort: 80
        - name: https
          containerPort: 443
        livenessProbe:
          httpGet:
            path: /doku.php
            port: http
          initialDelaySeconds: 120
          periodSeconds: 10
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /doku.php
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          successThreshold: 1
          failureThreshold: 6
        resources:
          requests:
            cpu: 300m
            memory: 512Mi
          
        volumeMounts:
        - name: dokuwiki-data
          mountPath: /bitnami/dokuwiki
        - name: apache-data
          mountPath: /bitnami/apache
      volumes:
      - name: dokuwiki-data
        persistentVolumeClaim:
          claimName: RELEASE-NAME-dokuwiki-dokuwiki
      - name: apache-data
        persistentVolumeClaim:
          claimName: RELEASE-NAME-dokuwiki-apache

---
# Source: dokuwiki/templates/ingress.yaml


---
# Source: dokuwiki/templates/tls-secrets.yaml


