---
# Source: jasperreports/charts/mariadb/templates/secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: RELEASE-NAME-mariadb
  labels:
    app: "mariadb"
    chart: mariadb-5.2.3
    release: "RELEASE-NAME"
    heritage: "Tiller"
type: Opaque
data:
  mariadb-root-password: "dFF6QlJDTzc3cw=="
  
  mariadb-password: "aTBWNUV0WFloYg=="
---
# Source: jasperreports/templates/secrets.yaml
apiVersion: v1
kind: Secret
metadata:
  name: RELEASE-NAME-jasperreports
  labels:
    app: RELEASE-NAME-jasperreports
    chart: "jasperreports-4.0.0"
    release: "RELEASE-NAME"
    heritage: "Tiller"
type: Opaque
data:
  
  jasperreports-password: "c0J5STBYRncyNw=="
  
  smtp-password: ""

---
# Source: jasperreports/charts/mariadb/templates/master-configmap.yaml

apiVersion: v1
kind: ConfigMap
metadata:
  name: RELEASE-NAME-mariadb
  labels:
    app: mariadb
    component: "master"
    chart: mariadb-5.2.3
    release: "RELEASE-NAME"
    heritage: "Tiller"
data:
  my.cnf: |-
    [mysqld]
    skip-name-resolve
    explicit_defaults_for_timestamp
    basedir=/opt/bitnami/mariadb
    port=3306
    socket=/opt/bitnami/mariadb/tmp/mysql.sock
    tmpdir=/opt/bitnami/mariadb/tmp
    max_allowed_packet=16M
    bind-address=0.0.0.0
    pid-file=/opt/bitnami/mariadb/tmp/mysqld.pid
    log-error=/opt/bitnami/mariadb/logs/mysqld.log
    character-set-server=UTF8
    collation-server=utf8_general_ci
    
    [client]
    port=3306
    socket=/opt/bitnami/mariadb/tmp/mysql.sock
    default-character-set=UTF8
    
    [manager]
    port=3306
    socket=/opt/bitnami/mariadb/tmp/mysql.sock
    pid-file=/opt/bitnami/mariadb/tmp/mysqld.pid
---
# Source: jasperreports/charts/mariadb/templates/tests.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: RELEASE-NAME-mariadb-tests
data:
  run.sh: |-
    @test "Testing MariaDB is accessible" {
      mysql -h RELEASE-NAME-mariadb -uroot -p$MARIADB_ROOT_PASSWORD -e 'show databases;'
    }

---
# Source: jasperreports/templates/pvc.yaml
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: RELEASE-NAME-jasperreports
  labels:
    app: RELEASE-NAME-jasperreports
    chart: "jasperreports-4.0.0"
    release: "RELEASE-NAME"
    heritage: "Tiller"
spec:
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "8Gi"
---
# Source: jasperreports/charts/mariadb/templates/master-svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: RELEASE-NAME-mariadb
  labels:
    app: "mariadb"
    component: "master"
    chart: mariadb-5.2.3
    release: "RELEASE-NAME"
    heritage: "Tiller"
spec:
  type: ClusterIP
  ports:
  - name: mysql
    port: 3306
    targetPort: mysql
  selector:
    app: "mariadb"
    component: "master"
    release: "RELEASE-NAME"

---
# Source: jasperreports/templates/svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: RELEASE-NAME-jasperreports
  labels:
    app: RELEASE-NAME-jasperreports
    chart: "jasperreports-4.0.0"
    release: "RELEASE-NAME"
    heritage: "Tiller"
spec:
  type: LoadBalancer
  externalTrafficPolicy: "Cluster"
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:
    app: RELEASE-NAME-jasperreports

---
# Source: jasperreports/charts/mariadb/templates/test-runner.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "RELEASE-NAME-mariadb-test-nhb7k"
  annotations:
    "helm.sh/hook": test-success
spec:
  initContainers:
    - name: "test-framework"
      image: "dduportal/bats:0.4.0"
      command:
        - "bash"
        - "-c"
        - |
          set -ex
          # copy bats to tools dir
          cp -R /usr/local/libexec/ /tools/bats/
      volumeMounts:
      - mountPath: /tools
        name: tools
  containers:
    - name: mariadb-test
      image: docker.io/bitnami/mariadb:10.1.37
      imagePullPolicy: "IfNotPresent"
      command: ["/tools/bats/bats", "-t", "/tests/run.sh"]
      env:
        - name: MARIADB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: RELEASE-NAME-mariadb
              key: mariadb-root-password
      volumeMounts:
      - mountPath: /tests
        name: tests
        readOnly: true
      - mountPath: /tools
        name: tools
  volumes:
  - name: tests
    configMap:
      name: RELEASE-NAME-mariadb-tests
  - name: tools
    emptyDir: {}
  restartPolicy: Never

---
# Source: jasperreports/templates/deployment.yaml
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: RELEASE-NAME-jasperreports
  labels:
    app: RELEASE-NAME-jasperreports
    chart: "jasperreports-4.0.0"
    release: "RELEASE-NAME"
    heritage: "Tiller"
spec:
  selector:
    matchLabels:
      app: RELEASE-NAME-jasperreports
      release: "RELEASE-NAME"
  template:
    metadata:
      labels:
        app: RELEASE-NAME-jasperreports
        chart: "jasperreports-4.0.0"
        release: "RELEASE-NAME"
    spec:
      containers:
      - name: RELEASE-NAME-jasperreports
        image: docker.io/bitnami/jasperreports:7.1.0
        imagePullPolicy: "IfNotPresent"
        env:
        - name: ALLOW_EMPTY_PASSWORD
          value: "yes"
        - name: MARIADB_HOST
          value: RELEASE-NAME-mariadb
        - name: MARIADB_PORT_NUMBER
          value: "3306"
        - name: JASPERREPORTS_DATABASE_NAME
          value: "bitnami_jasperreports"
        - name: JASPERREPORTS_DATABASE_USER
          value: "bn_jasperreports"
        - name: JASPERREPORTS_DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: RELEASE-NAME-mariadb
              key: mariadb-password
        - name: JASPERREPORTS_USERNAME
          value: "user"
        - name: JASPERREPORTS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: RELEASE-NAME-jasperreports
              key: jasperreports-password
        - name: JASPERREPORTS_EMAIL
          value: "user@example.com"
        - name: SMTP_HOST
          value: 
        - name: SMTP_PORT
          value: 
        - name: SMTP_EMAIL
          value: 
        - name: SMTP_USER
          value: 
        - name: SMTP_PASSWORD
          valueFrom:
            secretKeyRef:
              name: RELEASE-NAME-jasperreports
              key: smtp-password
        - name: SMTP_PROTOCOL
          value: 
        ports:
        - name: http
          containerPort: 8080
        livenessProbe:
          httpGet:
            path: /jasperserver/login.html
            port: http
          initialDelaySeconds: 300
          timeoutSeconds: 5
          failureThreshold: 6
        readinessProbe:
          httpGet:
            path: /jasperserver/login.html
            port: http
          initialDelaySeconds: 30
          timeoutSeconds: 3
          periodSeconds: 5
        resources:
          requests:
            cpu: 300m
            memory: 512Mi
          
        volumeMounts:
        - name: jasperreports-data
          mountPath: /bitnami/jasperreports
      volumes:
      - name: jasperreports-data
        persistentVolumeClaim:
          claimName: RELEASE-NAME-jasperreports

---
# Source: jasperreports/charts/mariadb/templates/master-statefulset.yaml
apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
  name: RELEASE-NAME-mariadb
  labels:
    app: "mariadb"
    chart: mariadb-5.2.3
    component: "master"
    release: "RELEASE-NAME"
    heritage: "Tiller"
spec:
  selector:
    matchLabels:
      release: "RELEASE-NAME"
      component: "master"
      app: mariadb
  serviceName: "RELEASE-NAME-mariadb"
  replicas: 1
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: "mariadb"
        component: "master"
        release: "RELEASE-NAME"
        chart: mariadb-5.2.3
    spec:
      securityContext:
        fsGroup: 1001
        runAsUser: 1001
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 1
            podAffinityTerm:
              topologyKey: kubernetes.io/hostname
              labelSelector:
                matchLabels:
                  app: "mariadb"
                  release: "RELEASE-NAME"
      containers:
      - name: "mariadb"
        image: docker.io/bitnami/mariadb:10.1.37
        imagePullPolicy: "IfNotPresent"
        env:
        - name: MARIADB_ROOT_PASSWORD
          valueFrom:
            secretKeyRef:
              name: RELEASE-NAME-mariadb
              key: mariadb-root-password
        - name: MARIADB_USER
          value: "bn_jasperreports"
        - name: MARIADB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: RELEASE-NAME-mariadb
              key: mariadb-password
        - name: MARIADB_DATABASE
          value: "bitnami_jasperreports"
        ports:
        - name: mysql
          containerPort: 3306
        livenessProbe:
          exec:
            command: ["sh", "-c", "exec mysqladmin status -uroot -p$MARIADB_ROOT_PASSWORD"]
          initialDelaySeconds: 120
          periodSeconds: 10
          timeoutSeconds: 1
          successThreshold: 1
          failureThreshold: 3
        readinessProbe:
          exec:
            command: ["sh", "-c", "exec mysqladmin status -uroot -p$MARIADB_ROOT_PASSWORD"]
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 1
          successThreshold: 1
          failureThreshold: 3
        resources:
          {}
          
        volumeMounts:
        - name: data
          mountPath: /bitnami/mariadb
        - name: config
          mountPath: /opt/bitnami/mariadb/conf/my.cnf
          subPath: my.cnf
      volumes:
        - name: config
          configMap:
            name: RELEASE-NAME-mariadb
  volumeClaimTemplates:
    - metadata:
        name: data
        labels:
          app: "mariadb"
          component: "master"
          release: "RELEASE-NAME"
          heritage: "Tiller"
      spec:
        accessModes:
          - "ReadWriteOnce"
        resources:
          requests:
            storage: "8Gi"

---
# Source: jasperreports/charts/mariadb/templates/initialization-configmap.yaml

---
# Source: jasperreports/charts/mariadb/templates/slave-configmap.yaml


---
# Source: jasperreports/charts/mariadb/templates/slave-statefulset.yaml


---
# Source: jasperreports/charts/mariadb/templates/slave-svc.yaml


---
# Source: jasperreports/templates/externaldb-secrets.yaml

