---
# Source: openiban/templates/serviceaccount.yaml

apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app: openiban
    chart: "openiban-1.0.0"
    heritage: Tiller
    release: RELEASE-NAME
  name: RELEASE-NAME-openiban
---
# Source: openiban/templates/role.yaml

kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: RELEASE-NAME-openiban
  labels:
    app: openiban
    chart: openiban-1.0.0
    release: "RELEASE-NAME"
    heritage: "Tiller"
rules:
- apiGroups: [""]
  resources: ["endpoints"]
  verbs: ["get"]

---
# Source: openiban/templates/rolebinding.yaml

kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: RELEASE-NAME-openiban
  labels:
    app: openiban
    chart: openiban-1.0.0
    release: "RELEASE-NAME"
    heritage: "Tiller"
subjects:
- kind: ServiceAccount
  name: RELEASE-NAME-openiban
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: RELEASE-NAME-openiban

---
# Source: openiban/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: RELEASE-NAME-openiban
  labels:
    app: openiban
    chart: openiban-1.0.0
    release: RELEASE-NAME
    heritage: Tiller
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: openiban
    release: RELEASE-NAME

---
# Source: openiban/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: RELEASE-NAME-openiban
  labels:
    app: openiban
    chart: openiban-1.0.0
    release: RELEASE-NAME
    heritage: Tiller
spec:
  replicas: 1
  selector:
    matchLabels:
      app: openiban
      release: RELEASE-NAME
  template:
    metadata:
      labels:
        app: openiban
        release: RELEASE-NAME
    spec:
      securityContext:
        fsGroup: 1000
        runAsUser: 1000
      containers:
        - name: openiban
          image: "fourcube/openiban:1.0.1"
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /validate/DE89370400440532013000
              port: http
            initialDelaySeconds: 5
          readinessProbe:
            httpGet:
              path: /validate/DE89370400440532013000
              port: http
          resources:
            {}
            

---
# Source: openiban/templates/ingress.yaml


