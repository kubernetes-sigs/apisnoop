---
# Source: cerebro/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: RELEASE-NAME-cerebro
  labels:
    app: cerebro
    chart: cerebro-0.4.0
    release: RELEASE-NAME
    heritage: Tiller
data:
  application.conf: |-
    secret = "uFN8RbtNXURcsFk26mKJBk8NoiHqtIdiOTDQJKcDsOvnetx8PYZm2Mz0qCfN9etH"

    basePath = "/"

    pidfile.path = /dev/null

    rest.history.size = 50

    data.path = "/var/db/cerebro/cerebro.db"

    es = {
      gzip = true
    }

    auth = {
    }

    hosts = [
    ]

---
# Source: cerebro/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: RELEASE-NAME-cerebro
  labels:
    app: cerebro
    chart: cerebro-0.4.0
    release: RELEASE-NAME
    heritage: Tiller
  annotations:
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: cerebro
    release: RELEASE-NAME

---
# Source: cerebro/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: RELEASE-NAME-cerebro
  labels:
    app: cerebro
    chart: cerebro-0.4.0
    release: RELEASE-NAME
    heritage: Tiller
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: cerebro
      release: RELEASE-NAME
  template:
    metadata:
      labels:
        app: cerebro
        release: RELEASE-NAME
      annotations:
        checksum/config: 3f4c7fbff761e3d837a2d9d711826f11977f72f7428562cebaadf627c60e4a8b
    spec:
      initContainers:
        - name: chown-db
          image: docker.io/busybox:musl
          command: ["chown", "1000:1000", "/var/db/cerebro"]
          volumeMounts:
            - name: db
              mountPath: /var/db/cerebro
      containers:
        - name: cerebro
          image: "lmenezes/cerebro:0.8.1"
          imagePullPolicy: IfNotPresent
          args: ["-Dconfig.file=/etc/cerebro/application.conf"]
          ports:
            - name: http
              containerPort: 9000
              protocol: TCP
          volumeMounts:
            - name: db
              mountPath: /var/db/cerebro
            - name: config
              mountPath: /etc/cerebro
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {}
            
      volumes:
        - name: db
          emptyDir: {}
        - name: config
          configMap:
            name: RELEASE-NAME-cerebro

---
# Source: cerebro/templates/ingress.yaml


