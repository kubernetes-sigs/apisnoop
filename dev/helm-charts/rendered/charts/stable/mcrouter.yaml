---
# Source: mcrouter/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: RELEASE-NAME-mcrouter
  labels:
    app: mcrouter
    chart: mcrouter-0.1.1
    release: RELEASE-NAME
    heritage: Tiller
data:
  config.json: |-
    {
      "pools": {
        "A": {
          "servers": [
            "RELEASE-NAME-memcached-0.RELEASE-NAME-memcached.hub.svc.cluster.local:11211",
            "RELEASE-NAME-memcached-1.RELEASE-NAME-memcached.hub.svc.cluster.local:11211",
            "RELEASE-NAME-memcached-2.RELEASE-NAME-memcached.hub.svc.cluster.local:11211",
          ]
        }
      },
      "route": "PoolRoute|A"
    }
---
# Source: mcrouter/charts/memcached/templates/svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: RELEASE-NAME-memcached
  labels:
    app: RELEASE-NAME-memcached
    chart: "memcached-1.2.1"
    release: "RELEASE-NAME"
    heritage: "Tiller"
spec:
  clusterIP: None
  ports:
  - name: memcache
    port: 11211
    targetPort: memcache
  selector:
    app: RELEASE-NAME-memcached

---
# Source: mcrouter/templates/svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: RELEASE-NAME-mcrouter
  labels:
    app: RELEASE-NAME-mcrouter
    chart: "mcrouter-0.1.1"
    release: "RELEASE-NAME"
    heritage: "Tiller"
spec:
  clusterIP: None
  ports:
  - name: mcrouter-port
    port: 5000
    targetPort: mcrouter-port
  selector:
    app: RELEASE-NAME-mcrouter

---
# Source: mcrouter/templates/daemonset.yaml

apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  name: RELEASE-NAME-mcrouter
  labels:
    app: RELEASE-NAME-mcrouter
    chart: "mcrouter-0.1.1"
    release: "RELEASE-NAME"
  annotations:
    checksum/config: b829b7dc33c818b61197decd29dc87a7061ba9808d29e605286600ca0e4364bd
spec:
  template:
    metadata:
      labels:
        app: RELEASE-NAME-mcrouter
        chart: "mcrouter-0.1.1"
        release: "RELEASE-NAME"
    spec:
      containers:
      - name: RELEASE-NAME-mcrouter
        image: jphalip/mcrouter:0.36.0
        imagePullPolicy: ""
        command: ["mcrouter"]
        args:
        - -p 5000
        - --config-file=/etc/mcrouter/config.json
        volumeMounts:
        - name: config
          mountPath: /etc/mcrouter
        ports:
        - name: mcrouter-port
          containerPort: 5000
          hostPort: 5000
        livenessProbe:
          tcpSocket:
            port: mcrouter-port
          initialDelaySeconds: 30
          timeoutSeconds: 5
        readinessProbe:
          tcpSocket:
            port: mcrouter-port
          initialDelaySeconds: 5
          timeoutSeconds: 1
        resources:
          limits:
            cpu: 256m
            memory: 512Mi
          requests:
            cpu: 100m
            memory: 128Mi
          
      volumes:
      - name: config
        configMap:
          name: RELEASE-NAME-mcrouter
---
# Source: mcrouter/charts/memcached/templates/statefulset.yaml
apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
  name: RELEASE-NAME-memcached
  labels:
    app: RELEASE-NAME-memcached
    chart: "memcached-1.2.1"
    release: "RELEASE-NAME"
    heritage: "Tiller"
spec:
  serviceName: RELEASE-NAME-memcached
  replicas: 3
  template:
    metadata:
      labels:
        app: RELEASE-NAME-memcached
        chart: "memcached-1.2.1"
        release: "RELEASE-NAME"
        heritage: "Tiller"
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - topologyKey: "kubernetes.io/hostname"
            labelSelector:
              matchLabels:
                app:  RELEASE-NAME-memcached
                release: "RELEASE-NAME"
      containers:
      - name: RELEASE-NAME-memcached
        image: memcached:1.4.36-alpine
        imagePullPolicy: ""
        command:
        - memcached
        - -m 64
        - -o
        - modern
        - -v
        ports:
        - name: memcache
          containerPort: 11211
        livenessProbe:
          tcpSocket:
            port: memcache
          initialDelaySeconds: 30
          timeoutSeconds: 5
        readinessProbe:
          tcpSocket:
            port: memcache
          initialDelaySeconds: 5
          timeoutSeconds: 1
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          

---
# Source: mcrouter/charts/memcached/templates/pdb.yaml
apiVersion: policy/v1beta1
kind: PodDisruptionBudget
metadata:
  name: RELEASE-NAME-memcached
spec:
  selector:
    matchLabels:
      app: RELEASE-NAME-memcached
      chart: "memcached-1.2.1"
      release: "RELEASE-NAME"
      heritage: "Tiller"
  minAvailable: 3
  
---
# Source: mcrouter/templates/statefulset.yaml


