const D3FlareData = require('../sample-data/flareData.json')

const initialState = {
  data: D3FlareData
}

export default (state = initialState, action = {}) => {
  switch (action.type) {
  default:
    return state
  }
}
