apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubemacs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubemacs
  template:
    metadata:
      labels:
        app: kubemacs
    spec:
      serviceAccountName: kubemacs-sa
      containers:
      - name: kubemacs
        image: gcr.io/apisnoop/kubemacs:0.9.24
        env:
          - name: TZ
            valueFrom:
              configMapKeyRef:
                name: kubemacs-configuration
                key: TZ
          - name: GIT_COMMITTER_EMAIL
            valueFrom:
              configMapKeyRef:
                name: kubemacs-configuration
                key: GIT_EMAIL
          - name: GIT_COMMITTER_NAME
            valueFrom:
              configMapKeyRef:
                name: kubemacs-configuration
                key: GIT_NAME
          - name: GIT_AUTHOR_EMAIL
            valueFrom:
              configMapKeyRef:
                name: kubemacs-configuration
                key: GIT_EMAIL
          - name: GIT_AUTHOR_NAME
            valueFrom:
              configMapKeyRef:
                name: kubemacs-configuration
                key: GIT_NAME
        volumeMounts:
          - mountPath: '/home/ii/workspace'
            name: kubemacs-hostpath
          - name: docker
            mountPath: /var/run/docker.sock
          - name: host-tmp
            mountPath: /tmp
      volumes:
        - name: kubemacs-hostpath
          hostPath:
            path: /workspace
        - name: docker
          hostPath:
            path: /var/run/docker.sock
            type: Socket
        - name: host-tmp
          hostPath:
            path: /var/host/tmp

