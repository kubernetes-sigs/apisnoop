type Query {
    info: String!
    feed: [AuditLog!]!
}

type AuditLog {
  id: ID!
  job: String
  bucket: String!
  version: String!
  job-version: String!
  infra-commit: String!
  pod: String!
  passed: Boolean
  result: String!
  timestamp: String
}
