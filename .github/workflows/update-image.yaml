name: Update image

on:
  push:
    branches:
      - "main"

jobs:
  update:
    runs-on: ubuntu-latest
    name: Update the commits!
    steps:
      - name: echo test
        uses: actions/checkout@v3
        run: echo "$(git rev-parse --short=6 $GITHUB_SHA)"
